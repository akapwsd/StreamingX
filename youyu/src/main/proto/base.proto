syntax = "proto3";
package uyujoy.com.api.gateway.frontend;
option go_package = "uyujoy.com/pass/protogen/gateway;gateway";

message messageFrame {
  uint32 crc32 = 1;
  bytes data = 2; //这里的data是用protobuf转成二进制的结构体，该结构体就是crcId对应的类型
}
